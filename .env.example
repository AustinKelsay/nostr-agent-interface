# Nostr Agent Interface configuration example
#
# Precedence model:
# 1) Explicit tool/API/CLI args
# 2) Environment variables below
# 3) Built-in defaults

# Default relay list used by tools when `relays` is not passed.
# Supports either comma-separated values:
# NOSTR_DEFAULT_RELAYS=wss://relay.damus.io,wss://nos.lol
# Or JSON string array:
# NOSTR_DEFAULT_RELAYS=["wss://relay.damus.io","wss://nos.lol"]
NOSTR_DEFAULT_RELAYS=

# API mode config (overridden by --host / --port flags)
NOSTR_AGENT_API_HOST=127.0.0.1
NOSTR_AGENT_API_PORT=3030
# Optional API key for protecting /tools endpoints.
# Clients may send either:
# - x-api-key: <token>
# - authorization: Bearer <token>
# Health endpoint (/health) remains public for probes.
NOSTR_AGENT_API_KEY=
# Optional in-memory fixed-window rate limiting for /tools endpoints.
# Set max to 0 to disable rate limiting.
NOSTR_AGENT_API_RATE_LIMIT_MAX=120
NOSTR_AGENT_API_RATE_LIMIT_WINDOW_MS=60000
# Structured API audit logs (JSON lines)
NOSTR_AGENT_API_AUDIT_LOG_ENABLED=true
# Include redacted request/response bodies in audit logs.
# Production recommendation: set to false unless body-level auditing is required.
NOSTR_AGENT_API_AUDIT_LOG_INCLUDE_BODIES=true

# MCP child process config for CLI/API wrappers.
# If unset, wrappers auto-discover local project entrypoints.
NOSTR_MCP_COMMAND=
# Supports either whitespace-separated args or JSON string array.
# Example JSON: ["build/index.js"]
NOSTR_MCP_ARGS=
