# Nostr Agent Interface configuration example
#
# Precedence model:
# 1) Explicit tool/API/CLI args
# 2) Environment variables below
# 3) Built-in defaults

# Default relay list used by tools when `relays` is not passed.
# Supports either comma-separated values:
# NOSTR_DEFAULT_RELAYS=wss://relay.damus.io,wss://nos.lol
# Or JSON string array:
# NOSTR_DEFAULT_RELAYS=["wss://relay.damus.io","wss://nos.lol"]
NOSTR_DEFAULT_RELAYS=

# API mode config (overridden by --host / --port flags)
NOSTR_AGENT_API_HOST=127.0.0.1
NOSTR_AGENT_API_PORT=3030

# MCP child process config for CLI/API wrappers.
# If unset, wrappers auto-discover local project entrypoints.
NOSTR_MCP_COMMAND=
# Supports either whitespace-separated args or JSON string array.
# Example JSON: ["build/index.js"]
NOSTR_MCP_ARGS=
